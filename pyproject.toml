[project]
name = "brahmanda"
version = "0.1.0"
description = "A Vedic Universe Simulation Engine — research tool for simulation theory"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "httpx>=0.27.0",
    "pydantic>=2.0",
    "rich>=13.0",
]

[project.scripts]
brahmanda = "brahmanda.main:main"
